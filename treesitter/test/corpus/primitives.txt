================================================================================
Primitive literals are expressions and patterns
================================================================================

let f = fn x => match x with
  | 1i => 1i
  | 1.5 => 1.5
  | "text" => "text"
  | true => false
  | _ => true
  end

--------------------------------------------------------------------------------

(source_file
  (let_definition
    (identifier)
    (function
      (identifier)
      (match_expression
        (identifier)
        (match_arm
          (natural)
          (natural))
        (match_arm
          (natural)
          (natural))
        (match_arm
          (string)
          (string))
        (match_arm
          (boolean)
          (boolean))
        (match_arm
          (wildcard)
          (boolean))))))

================================================================================
Raw string lines are one string, and a comment between them makes two
================================================================================

extern f : fn(Int) -> Int =
  \\(x) => {
  \\  return x -- not a comment
  \\}

let s = \\one line

let t = \\a
-- between
let u = \\b

--------------------------------------------------------------------------------

(source_file
  (extern_definition
    (identifier)
    (extern_annotation
      (extern_function_type
        (identifier)
        (identifier)))
    (string))
  (let_definition
    (identifier)
    (string))
  (let_definition
    (identifier)
    (string))
  (line_comment)
  (let_definition
    (identifier)
    (string)))

================================================================================
Fixed-width signed literals remain atoms in arguments and patterns
================================================================================

let value : Int8 = -128i8
let result = f -128i8 127i8
let choose = fn x => match x with | -128i8 => 255n8 | _ => 0n8 end
let largest : Nat64 = 18446744073709551615n64

--------------------------------------------------------------------------------

(source_file
  (let_definition
    (identifier)
    (annotation (identifier))
    (natural))
  (let_definition
    (identifier)
    (application
      (application (identifier) (natural))
      (natural)))
  (let_definition
    (identifier)
    (function
      (identifier)
      (match_expression
        (identifier)
        (match_arm (natural) (natural))
        (match_arm (wildcard) (natural)))))
  (let_definition
    (identifier)
    (annotation (identifier))
    (natural)))
