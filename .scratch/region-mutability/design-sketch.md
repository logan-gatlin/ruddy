# Single-region mutability

The finalized, authoritative specification is [spec.md](spec.md).

It consolidates the selected syntax, caller-region allocation, implicit
function-body isolation, effect-less-initializer generalization, and trusted
FFI contract. It also contains implementation decisions and acceptance criteria.

The earlier exploration remains available in [investigation.md](investigation.md),
[escaping-state.md](escaping-state.md), [polymorphism.md](polymorphism.md),
[ffi.md](ffi.md), and [prior-art.md](prior-art.md). Deferred alternatives in those
notes are not implementation requirements.
