# 03 Inference over effect arguments

Status: resolved
Type: task

Argument tuples as label payloads; fresh instantiation at operation references and handlers;
annotation arguments; coalescing and the dedicated effect-argument mismatch error with causal
detail; generalization.

Seams: core compilation accepted schemes and errors (`tests/src/compile.rs`), `tests/src/inference.rs`.
