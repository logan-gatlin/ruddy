# 04 Diagnostics and printing

Status: open
Type: task

Codes, titles, spans, help for arity, kind, lacks, alias-cycle, modifier and inferred-argument
errors; applied effects rendered in semantic types and debugger views; print round trips.

Seams: `tests/src/ui.rs` inventory and goldens, `tests/src/print.rs`, `tests/src/stage.rs`.
