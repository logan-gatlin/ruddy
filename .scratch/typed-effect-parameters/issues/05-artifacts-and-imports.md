# 05 Artifact schema and dependency imports

Status: resolved
Type: task

Breaking artifact schema with ordered effect parameters, generic interfaces and unexpanded alias
rows; strict validation and tolerant recovery; import of parameterized effects and aliases.

Seams: `tests/src/artifact.rs` round trips and malformed data, dependency compilation tests.
