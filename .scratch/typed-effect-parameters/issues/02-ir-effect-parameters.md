# 02 IR effect parameters, arity, kinds and alias expansion

Status: open
Type: task

Effect declarations bind parameters; labels carry arguments; arity errors; kind inference spans
effects; row-argument checks cover effect arguments; parameterized alias expansion with cycles
and modifiers; structural identity includes parameters and arguments.

Seams: core compilation (`tests/src/compile.rs`) structured errors and accepted IR; `tests/src/ir.rs`.
