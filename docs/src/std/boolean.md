---
doc: true
layout: std.njk
stdReference: true
---

# [std](bundle.md)::boolean

`boolean` provides logical operations and ordering for `Bool` values.

An implication can require a true conclusion only when its premise is true.

```ruddy
let access_allowed =
  std::boolean::implies account_is_private viewer_follows_account
```

## Values

### compare

```ruddy
let compare: order::Ordering Bool
```

Orders `false` before `true`.

### equal

```ruddy
let equal: Bool -> Bool -> Bool
```

The Boolean comparisons derived from the three-way ordering.

### greater_than

```ruddy
let greater_than: Bool -> Bool -> Bool
```

The Boolean comparisons derived from the three-way ordering.

### greater_than_or_equal

```ruddy
let greater_than_or_equal: Bool -> Bool -> Bool
```

The Boolean comparisons derived from the three-way ordering.

### implies

```ruddy
let implies: Bool -> Bool -> Bool
```

Whether a true premise has a true conclusion.

### less_than

```ruddy
let less_than: Bool -> Bool -> Bool
```

The Boolean comparisons derived from the three-way ordering.

### less_than_or_equal

```ruddy
let less_than_or_equal: Bool -> Bool -> Bool
```

The Boolean comparisons derived from the three-way ordering.

### logical_and

```ruddy
let logical_and: Bool -> Bool -> Bool
```

Whether both values are `true`.

### logical_not

```ruddy
let logical_not: Bool -> Bool
```

The logical negation.

### logical_or

```ruddy
let logical_or: Bool -> Bool -> Bool
```

Whether either value is `true`.

### logical_xor

```ruddy
let logical_xor: Bool -> Bool -> Bool
```

Whether exactly one value is `true`.

### not_equal

```ruddy
let not_equal: Bool -> Bool -> Bool
```

The Boolean comparisons derived from the three-way ordering.

<!-- Generated by ruddy doc for std. -->
